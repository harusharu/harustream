# pnpm settings for this single package (not a monorepo).
# `allowBuilds` (pnpm >= 10.26 / 11) replaces the removed
# `onlyBuiltDependencies` allowlist.
allowBuilds:
  esbuild: set this to true or false
  msw: true
